org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Tell Gradle which full JDK (with javac) to use. The system's
# /usr/lib/jvm install is JRE-only Java 21, so :app:compileDebugJavaWithJavac
# fails with "Toolchain installation ... does not provide the required
# capabilities: [JAVA_COMPILER]". This path is the JDK Gradle already
# auto-downloaded for toolchain resolution. Override per machine, or install
# openjdk-21-jdk system-wide and remove this line.
org.gradle.java.home=/home/quocanh/.gradle/jdks/amazon_com_inc_-21-amd64-linux.2
org.gradle.java.installations.auto-detect=false
